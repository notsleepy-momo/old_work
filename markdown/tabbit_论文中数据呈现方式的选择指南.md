### **建议在论文中同时使用折线图（主推）与详细表格，以折线图展现性能随噪声水平下降的趋势，并辅以表格记录精确数值与显著性检验结果。**

根据您提供的文档 `@noise_robustness_20260728_091831.txt`，该实验核心在于探讨 **设备坐标扰动（$\sigma_{global}$）对模型性能（CLS、RTA、FNA 等）的影响**。这种“性能随干扰强度变化”的数据在学术论文中最标准的呈现方式是**折线图（Line Plot）**，因为它能直观反映模型的“鲁棒性边界”。

以下是详细的呈现方案建议：

### **可视化方案：带阴影的折线图（Error Bar / Shaded Area Plot）**

折线图是表现“鲁棒性”的首选，能够清晰展示随着 $\sigma_{global}$ 增大，性能指标如何平缓或剧烈下降。

**1. 绘图关键要素**
*   **横轴（X-axis）：** 扰动强度 $\sigma_{global}$ (dm)，从 0.00 到 6.67。
*   **纵轴（Y-axis）：** 指标百分比（%）。建议将 **CLS** 作为主线，或者在同一张图中绘制多条线（CLS, RTA, FNA），用不同颜色区分。
*   **误差区间（Error Shading）：** 由于数据包含 `±std`（标准差），**强烈建议**在折线周围绘制半透明的阴影区域（Shaded Area）。这在论文中能体现实验的严谨性，展示模型在不同 Seed 下的稳定性。
*   **关键阈值线：** 文档中提到了一个警示项 `[WARN] σ_g=3.33dm 内 CLS 保持率 70.7% (< 90%)`。你可以在图中 $\sigma_g=3.33$ 处画一条垂直虚线，或在纵轴 90% 处画水平参考线，通过视觉对比突出模型在该噪声水平下已跌破预设的鲁棒性阈值。

**2. 推荐的图表布局**
*   **方案 A（综合图）：** 同一坐标系内绘制 CLS、RTA 和 FNA 三条线。
*   **方案 B（对比图）：** 左侧画 CLS 随 $\sigma_g$ 变化的绝对值趋势；右侧画 **CLS Retention**（保持率）的趋势，以强调“性能衰减速度”。

### **表格呈现方案：详细指标与统计显著性**

虽然图表直观，但表格能承载更多统计细节（如 Wilcoxon 检验结果），通常作为附表或紧随图后的定量分析。

**1. 表格设计建议**
*   **合并指标：** 建议保留原始文档的结构，但增加一列 `p-value` 或使用星号标注显著性。
*   **突出重点：** 将 $\sigma_g = 0$ 作为 Baseline（基准线），后续行对比展示 Retention 比例。

**2. 论文中的排版样式（示例）**
您可以参考以下 LaTeX/Markdown 风格的排版：

| $\sigma_{global}$ (dm) | CLS (%) $\uparrow$ | RTA (%) $\uparrow$ | FNA (%) $\uparrow$ | CLS Retention | $p$-value |
| :--- | :--- | :--- | :--- | :--- | :--- |
| 0.00 (Base) | 90.0 | 0.0 | 0.0 | 100.0% | — |
| 0.67 | 69.6 $\pm$ 5.4 | 77.1 $\pm$ 7.8 | 61.4 $\pm$ 9.6 | 77.3% | 0.062 |
| 3.33 | 63.6 $\pm$ 8.4 | 71.4 $\pm$ 10.1 | 52.4 $\pm$ 15.0 | 70.7% | 0.062 |
| 6.67 | 53.7 $\pm$ 14.9 | 51.4 $\pm$ 23.9 | 39.9 $\pm$ 22.3 | 59.7% | 0.062 |

### **论文中的文字描述策略**

在呈现图表后，建议配合以下逻辑进行分析：

1.  **趋势描述：** “随着扰动强度 $\sigma_{global}$ 从 0 增加至 6.67 dm，综合性能指标 CLS 呈现明显的单调下降趋势。”
2.  **鲁棒性分析：** “在轻微噪声（$\sigma_g = 0.67$）下，CLS 保持率迅速跌至 77.3%，表明模型对初始坐标偏移较为敏感。”
3.  **关键失效点：** 特别指出在 3.33 dm 时，模型已无法满足 90% 的性能保持率要求（仅为 70.7%），这为后续改进（如 B3 中的真实环境退化增强）提供了实验依据。
4.  **统计结论：** “尽管性能大幅下降，但由于多种子实验的方差较大，Wilcoxon 符号秩检验显示 $p=0.062$，在 $\alpha=0.05$ 水平下尚未达到极显著差异，这暗示了模型在极端噪声下的不稳定性（High Variance）。”

### **总结建议**
*   **如果版面有限：** 优先画一张 **带误差阴影的折线图**，展示 CLS 和 CLS Retention。
*   **如果强调严谨性：** 使用折线图展示趋势，并在正文或附录中放入完整的表格以供同行查阅精确的 $\pm$ std 数值。

根据文档 `@noise_robustness_20260728_091831.txt` 记录的实验数据，从学术研究和算法评测的角度来看，这份表格的结果存在**明显的逻辑矛盾**和**统计学上的异常**。

简单来说：**这份数据在逻辑上是不合理的，甚至可能存在实验代码 Bug 或统计计算错误。**

以下是详细的审查分析：

### **1. 核心指标逻辑冲突（RTA 与 FNA 的突变）**
这是最不合理的地方：
*   **现象：** 当 $\sigma_g = 0.00$（无噪声）时，RTA（召回率相关）和 FNA（误报率相关）竟然全是 **0.0%**，但综合指标 CLS 却是最高的 **90.0%**。
*   **矛盾点：** 根据文档末尾的公式：`CLS = 0.45 x FNA + 0.35 x F1_IoU + 0.20 x RTA`。
    *   如果 FNA 和 RTA 都是 0，那么 CLS 要达到 90%，唯一的可能就是未列出的 `F1_IoU` 指标远超 100%（逻辑上不可能），或者公式在 $\sigma_g=0$ 时根本没生效。
    *   **正常情况：** 在无噪声的 Baseline 下，RTA 和 FNA 应该是该模型的最佳水平（接近 90%-100%），而不是 0。这说明 Baseline 的数据记录可能发生了错位。

### **2. 统计显著性（p-value）的异常一致性**
*   **现象：** 表格中从 $\sigma_g=0.67$ 到 $6.67$，所有的 Wilcoxon 检验结果全是 `p=0.062 n.s.`。
*   **矛盾点：** 
    *   $\sigma_g=0.67$ 时 CLS 是 69.6%，而 $\sigma_g=6.67$ 时 CLS 已经掉到了 53.7%，两者的均值和方差完全不同，但与 Baseline 对比的 p 值竟然完全一样。
    *   **原因分析：** `0.062` 这个数字通常出现在样本量极小（如 $n=5$）的非参数检验中。这说明无论噪声多大，统计检验都无法给出“显著”结论，这在学术论文中很难支撑“噪声对模型有显著影响”的论点。

### **3. 性能衰减的非线性特征**
*   **现象：** 
    *   从 $0.00 \to 0.67$，CLS 暴跌了 **20.4%**。
    *   从 $0.67 \to 1.67$，CLS 仅下降了 **0.8%**。
*   **矛盾点：** 这种“断崖式起步”随后又进入“平台期”的特征，通常意味着模型在面对任何一点点扰动时就会发生某种“系统性崩溃”（例如坐标系对齐失败），而不是随噪声增加而平滑衰减。

### **4. 实验预警未达标**
*   **现象：** 文档明确标注了 `[WARN] σ_g=3.33dm 内 CLS 保持率 70.7% (< 90%)`。
*   **结论：** 这说明该版本（v2）的改进并没有达到预期的鲁棒性目标。如果这是论文要展示的最终成果，那么它证明了模型**不具备**鲁棒性。

### **结论与修改建议**

**这份表格目前不建议直接放入论文，除非你打算讨论“模型为何在微小噪声下即刻失效”。**

**建议排查步骤：**
1.  **检查 Baseline 数据：** 确认 $\sigma_g=0$ 时 RTA 和 FNA 为 0 是否是记录错误。如果模型正常工作，这两个值不可能是 0。
2.  **检查 CLS 公式实现：** 核对代码里计算 CLS 的函数，看是否在噪声注入时改变了输入格式，导致计算逻辑与 Baseline 不一致。
3.  **增加样本量（Seeds）：** 现在的 $p=0.062$ 处于显著性边缘（卡在 0.05 之外），增加到 10 个 seed 可能有助于获得更稳定的统计学结论。
4.  **定位断崖式下跌原因：** 排查为何 $0.67$ dm 的微小扰动会导致性能从 90% 跌至 69%，这通常意味着模型缺乏数据增强或对坐标极度敏感。

*内容由 AI 生成仅供参考*